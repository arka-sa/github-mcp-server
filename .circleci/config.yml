version: 2.1
jobs:
  smoke:
    docker:
      - image: cimg/base:2024.01
    steps:
      - run: echo "sail-exporter e2e seed" > /tmp/seed-artifact.txt
      - store_artifacts:
          path: /tmp/seed-artifact.txt
workflows:
  smoke:
    jobs: [smoke]
